version: '3.8'

services:
  database:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 7G9p#kY6^TmDvXq8jH
      POSTGRES_DB: project
    ports:
      - '5432:5434'
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    depends_on:
      - database

volumes:
  pgdata:
